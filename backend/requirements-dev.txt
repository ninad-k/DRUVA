# =============================================================================
# DHRUVA Backend — Development Requirements
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# =============================================================================

-r requirements.txt

# --- Testing ---------------------------------------------------------------
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=6.0
pytest-mock>=3.14
pytest-xdist>=3.6
freezegun>=1.5
respx>=0.21               # httpx mocking
factory-boy>=3.3

# --- Lint / format / types -------------------------------------------------
ruff>=0.8
mypy>=1.13
types-redis
types-passlib

# --- Load testing ----------------------------------------------------------
locust>=2.32

# --- Pre-commit ------------------------------------------------------------
pre-commit>=4.0
